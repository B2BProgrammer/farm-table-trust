const assert = require('chai').assert;
const sinon = require('sinon');
const user = require('../../app/controllers/user-controller');

describe('User Controller Module : unit Testing', function () {

    it('should login successfully & generate token', function () {

    });

    it('should NOT login & generate error', function () {

    });
});